n = int(input())
arr = []
for i in range(n):
  element = int(input())
  if element > 2 or element < 0:
    print("Error")
  else: 
    arr.append(element)
print("The array before sorting: ",arr)
a = sorted(arr)
print("After Sorting: ",a)
